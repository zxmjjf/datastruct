由塔A借助塔B移动到塔C：主要逻辑

将n个盘子由A借助B移动到C (A,B,C,n)
  如果:n == 1 , 直接 A --> C
  否则：
      将n-1个盘子由A借助C移动到B （A，C，B，n-1) [递归调用】
      再将第n个盘子由A移动到C  （直接 A --> C) {这语句可以直接(A,B,C,1),但这样做影响性能}
      最后在将n-1个盘子由B借助A抵用到C （B，A，C，n-1）[递归调用]
